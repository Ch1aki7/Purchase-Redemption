# 项目二：资金流入流出预测系统 — 需求分析与最小可跑通方案

> 基于天池「资金流入流出预测」赛题（https://tianchi.aliyun.com/competition/entrance/231573/information）

---

## 一、项目目标一句话总结

基于余额宝 2013.07.01 ~ 2014.08.31 的用户交易数据 + 收益率 + Shibor 利率，预测 **2014年9月1日~9月30日** 每一天的**申购总额**和**赎回总额**，并输出符合赛题格式的预测结果文件。

---

## 二、数据资产全景（4张表）

### 表1：用户信息表 `user_profile_table`

| 字段 | 类型 | 说明 |
|---|---|---|
| user_id | bigint | 用户ID |
| sex | bigint | 1=男, 0=女 |
| city | bigint | 城市编码(脱敏) |
| constellation | string | 星座 |

→ 约 28,000 训练用户 + 约 2,000 测试用户（2014-09 首次出现）

### 表2：用户申购赎回表 `user_balance_table`（核心大表）

| 字段 | 类型 | 说明 |
|---|---|---|
| user_id | bigint | 用户ID |
| report_date | string | 日期(如 20140407) |
| tBalance | bigint | 今日余额(分) |
| yBalance | bigint | 昨日余额(分) |
| total_purchase_amt | bigint | 今日总申购 = 直接申购 + 收益 |
| direct_purchase_amt | bigint | 直接申购 |
| purchase_bal_amt | bigint | 支付宝余额申购 |
| purchase_bank_amt | bigint | 银行卡申购 |
| total_redeem_amt | bigint | 今日总赎回 = 消费 + 转账 |
| consume_amt | bigint | 消费金额 |
| transfer_amt | bigint | 转账金额 |
| tftobal_amt | bigint | 转入支付宝余额 |
| tftocard_amt | bigint | 转入银行卡 |
| share_amt | bigint | 当日收益 |
| category1~4 | bigint | 消费类别(consum_amt=0时为null) |

→ 约 280 万条记录，覆盖 2013.07.01 ~ 2014.08.31（427天）

**数据一致性约束**：`tBalance = yBalance + total_purchase_amt - total_redeem_amt`（无负值，已脱敏保证）

### 表3：收益率表 `mfd_day_share_interest`

| 字段 | 类型 | 说明 |
|---|---|---|
| mfd_date | string | 日期 |
| mfd_daily_yield | double | 万份收益(每万分收益，单位分) |
| mfd_7daily_yield | double | 七日年化收益率(%) |

### 表4：Shibor 利率表 `mfd_bank_shibor`

| 字段 | 类型 | 说明 |
|---|---|---|
| mfd_date | string | 日期 |
| Interest_O_N | double | 隔夜拆借(%) |
| Interest_1_W | double | 1周(%) |
| Interest_2_W | double | 2周(%) |
| Interest_1_M | double | 1月(%) |
| Interest_3_M | double | 3月(%) |
| Interest_6_M | double | 6月(%) |
| Interest_9_M | double | 9月(%) |
| Interest_1_Y | double | 1年(%) |

---

## 三、核心任务拆解

### 阶段 0：数据探索（先看看数据长什么样）

- 加载 4 张表，打印 shape、dtypes、前5行、缺失值、基本统计量
- 验证 `tBalance = yBalance + total_purchase_amt - total_redeem_amt`
- 画出训练时段（2013.07 ~ 2014.08）的每日申购/赎回总额趋势图

### 阶段 1：数据预处理 & 特征工程

1. **从 user_balance_table 聚合每日总量**
   - 按 `report_date` 分组，sum 得到每日 `total_purchase` 和 `total_redeem`
   - 这就是我们要预测的目标 y 序列

2. **合并收益率和利率**
   - 按日期 merge `mfd_day_share_interest` 和 `mfd_bank_shibor`
   - 形成一张「日期 + 申购总额 + 赎回总额 + 万份收益 + 7日年化 + 8个Shibor」的大宽表

3. **构造时间特征**
   - `dayofweek`（周一~周日 0~6）
   - `dayofmonth`（1~31）
   - `month`（1~12）
   - `is_weekend`（是否周末）
   - 申购/赎回的**滞后特征**（lag_1, lag_7, lag_30 等）
   - 申购/赎回的**滚动窗口统计**（rolling_mean_7, rolling_std_7 等）

4. **拆分训练/验证/测试**
   - 训练集：2013.07.01 ~ 2014.07.31（训练）
   - 验证集：2014.08.01 ~ 2014.08.31（调参验证）
   - 测试集：2014.09.01 ~ 2014.09.30（最终预测）

### 阶段 2：建模预测（最小方案用 LightGBM）

- **模型**：LightGBM（树模型，对表格数据效果好，训练快）
- **目标**：两个回归任务——`purchase` 和 `redeem`
  - 策略：分别训练两个模型 OR 用一个多输出模型 → 最小方案分别训练两个
- **训练**：在训练集上拟合，在验证集上看效果
- **预测**：对测试集 30 天做预测（滚动预测：每天预测完把预测值当 lag 特征传给下一天）

### 阶段 3：输出提交文件

输出 `tc_comp_predict_table.csv`，格式：

| report_date | purchase | redeem |
|---|---|---|
| 20140901 | 123456789 | 98765432 |
| ... | ... | ... |
| 20140930 | ... | ... |

- `purchase` 和 `redeem` 为整数（单位：分）
- 共 30 行

---

## 四、评估指标

每日相对误差：

> **申购误差：Purchase_i = |预测申购_i - 真实申购_i| / 真实申购_i**
> **赎回误差：Redeem_i = |预测赎回_i - 真实赎回_i| / 真实赎回_i**

得分函数 f(·) 单调递减：

- error = 0 → 10 分
- error > 0.3 → 0 分

**最终总分** = 申购得分 × 45% + 赎回得分 × 55%

> 注意：赛题不公布 f(·) 具体公式，但指导书明确了 error=0→10 分、error>0.3→0 分。我们可以近似用线性函数 `score = max(0, 10 × (1 - error / 0.3))` 来模拟评估。

---

## 五、指导书额外要求：可交互可视化分析系统

指导书要求做 4 个模块的 Web 可视化系统。作为「最小可跑通」方案，建议用 **Streamlit** 快速搭建单页应用，核心功能：

| 模块 | 最小实现 | Streamlit 组件 |
|---|---|---|
| 数据探索 | 历史申购/赎回趋势图、收益率变化图 | `st.line_chart` / plotly |
| 模型训练与评估 | 显示训练 loss、验证集对比 | `st.metric` + plotly |
| 预测结果展示 | 30天预测表格 + 折线图 | `st.dataframe` + `st.line_chart` |
| 误差分析 | 每日误差表、高亮大误差日 | `st.dataframe` 带颜色 |

---

## 六、最小可跑通方案：技术选型 & 目录结构

### 技术栈

| 环节 | 工具 |
|---|---|
| 数据处理 | Python + Pandas + NumPy |
| 建模 | LightGBM |
| 可视化系统 | Streamlit |
| 图表 | Plotly（交互式） |

### 建议目录结构

```
Purchase-Redemption/                         # 项目根目录
├── Purchase Redemption Data/                 # 数据读取目录（从平台下载的zip解压到这里）
│   ├── user_profile_table.csv
│   ├── user_balance_table.csv
│   ├── mfd_day_share_interest.csv
│   └── mfd_bank_shibor.csv
├── src/
│   ├── preprocess.py                         # 数据预处理 + 特征工程
│   ├── train.py                              # LightGBM 训练
│   ├── predict.py                            # 滚动预测 30 天
│   └── evaluate.py                           # 评估函数（模拟评分）
├── app.py                                    # Streamlit 可视化主入口
├── output/
│   └── tc_comp_predict_table.csv             # 最终提交文件
└── requirements.txt
```

### 依赖

```
pandas
numpy
lightgbm
scikit-learn
streamlit
plotly
```

---

## 七、代码执行流程（一次性跑通）

```
Purchase Redemption Data/  →  src/preprocess.py  →  生成 features.csv
                                      ↓
                                src/train.py  →  训练 LightGBM 模型 → 保存 model_purchase.pkl, model_redeem.pkl
                                      ↓
                                src/predict.py  →  滚动预测 9月30天 → output/tc_comp_predict_table.csv
                                      ↓
                                app.py  →  Streamlit 启动，可视化全流程结果
```

---

## 八、关键注意事项（踩坑预警）

| 风险点 | 对策 |
|---|---|
| 数据需从天池下载（注册后） | 提前注册天池账号并下载 zip 包（约23MB） |
| 9月是预测目标，没有真实标签用于验证 | 用 8月数据做本地验证集，9月预测直接输出 |
| 滞后特征在预测时需滚动计算 | predict.py 需要按天循环，每天预测完更新 lag 特征 |
| 申购/赎回值很大（日总量可能上亿分） | 训练前可做 log1p 变换，预测后 expm1 还原 |
| Streamlit 需要 Python 3.8+ | 确保环境兼容 |

---

## 九、数据下载指引

1. 访问 https://tianchi.aliyun.com/competition/entrance/231573/information
2. 注册/登录天池
3. 点击「报名」→ 进入赛题页 →「数据下载」→ 下载 `P*****a.zip`（约23MB）
4. 解压到 `Purchase-Redemption/Purchase Redemption Data/` 目录即可使用

---

## 十、实际实现方式与两轮优化结果

本章节记录项目最终落地的实现方式，以及在最小方案基础上的两轮优化过程。前九章为最初设计文档，本章节为实际执行结果。

### 10.1 实际实现方式

最小方案只规划了 LightGBM 单模型 + 基础时间特征，实际落地时做了如下扩展：

**特征工程（`src/preprocess.py`，46 → 113 个特征）**

| 特征类别 | 具体特征 | 解决的问题 |
|---|---|---|
| 周期傅里叶编码 | dayofweek / dayofmonth / month 的 sin/cos 变换 | 让模型感知周期连续性，比单纯整数编码更平滑 |
| 月初月末连续特征 | days_to_month_start/end、is_first/last_3/7_days、month_start/end_weight | 解决月初（8月1日）、月末（8月31日）误差爆炸问题 |
| 2014 节假日特征 | is_holiday、days_to_next/from_last_holiday、is_day_before/after_holiday | 捕捉节假日前后资金异动 |
| 历史同期特征 | same_day_last_month、same_weekday_w1~w4、mean_same_weekday_4w | 利用月度周期性和周度周期性 |
| 比率差分特征 | purchase_redeem_ratio、purchase/redeem_diff_lag1/lag7、net_inflow | 捕捉申购赎回相对关系和短期趋势 |
| 滚动统计增强 | 在 mean/std 基础上增加 min/max/median | 更完整的窗口分布信息 |
| 基础滞后/滚动 | lag_1/7/14/30、rolling_mean/std_7/14/30 | 原最小方案已有，保留 |

**建模（`src/train.py`）**

- **集成模型**：LightGBM + RandomForest 加权集成（`EnsembleModel` 类）
- **目标变换**：训练时 `log1p` 变换，预测时 `expm1` 还原，缓解大数值问题
- **早停机制**：LightGBM 用验证集做 100 轮早停，自动找最优迭代轮数
- **预测裁剪**：预测值裁剪到训练集 P1~P99 分位数区间，避免极端值

**预测（`src/predict.py`）**

- **滚动预测**：9 月每天预测完成后，将预测值回填为下一天的 lag 特征
- **外部变量处理**：9 月的收益率、Shibor 利率用 8 月均值填充（未来值未知）
- **平滑修正**：模型预测 × 0.85 + 近 7 日均值 × 0.15，缓解滚动漂移

### 10.2 第一轮优化：从 4.96 分到 8.05 分

**初始基线**：仅用基础时间特征 + 单一 LightGBM，验证集总分 **4.96**，其中申购评分 4.94、赎回评分 4.97，有 10 天误差超过 30%（零分日）。

**第一轮优化内容**：

1. **特征工程扩展**：实现上述 113 个特征，重点解决月初月末效应
   - 8 月 1 日申购误差从 53.2% 降到 13.2%
   - 8 月 31 日申购误差从 45.0% 降到 4.2%
2. **集成建模**：引入 LightGBM（权重 0.85）+ RandomForest（权重 0.15）加权集成
3. **早停 + 裁剪**：LightGBM 加 100 轮早停，预测值裁剪到历史合理区间
4. **滚动预测增强**：9 月外部变量用 8 月均值填充，加 0.85:0.15 平滑修正

**第一轮结果**：

| 指标 | 基线 | 第一轮后 |
|---|---|---|
| 申购 MAPE | 16.68% | 6.37% |
| 赎回 MAPE | 18.41% | 5.42% |
| 申购评分 | 4.94 | 7.88 |
| 赎回评分 | 4.97 | 8.19 |
| **总分** | **4.96** | **8.05** |
| 零分日（误差>30%） | 10 天 | 0 天 |

### 10.3 第二轮优化：从 8.05 分到 8.14 分

第二轮聚焦超参数调优，通过本地调参实验脚本遍历 9 种参数组合，找到最优配置后应用到 `src/train.py`。

**调参实验过程**：

遍历了以下维度的组合：

| 调参维度 | 实验取值 |
|---|---|
| LightGBM 集成权重 | 0.80 / 0.85 / 0.90 / 0.95 / 1.00 |
| 学习率 learning_rate | 0.005 / 0.003 |
| 树数量 n_estimators | 3000 / 5000 |
| 预测裁剪区间 | P1-P99 / P5-P95 |

**最优配置**：

| 参数 | 第一轮值 | 第二轮最优值 | 变化 |
|---|---|---|---|
| learning_rate | 0.005 | 0.003 | 降低，更慢更稳 |
| n_estimators | 3000 | 5000 | 增加，配合更低学习率 |
| LightGBM 权重 | 0.85 | 0.95 | 提高，让 LightGBM 主导 |
| RandomForest 权重 | 0.15 | 0.05 | 降低，RF 贡献有限 |

**关键发现**：

- Random Forest 在小数据集上贡献有限，权重降低反而提升总分
- 学习率降到 0.003 + 树数增到 5000，配合早停自动找最优迭代轮数，效果更好
- 裁剪区间收紧（P5-P95）单独使用无效果，与权重调整组合后也无额外收益

**第二轮结果**：

| 指标 | 第一轮 | 第二轮 | 提升 |
|---|---|---|---|
| 申购 MAPE | 6.37% | 6.01% | -0.36% |
| 赎回 MAPE | 5.42% | 5.22% | -0.20% |
| 申购评分 | 7.88 | 8.00 | +0.12 |
| 赎回评分 | 8.19 | 8.26 | +0.07 |
| **总分** | **8.05** | **8.14** | **+0.09** |
| 申购最大单日误差 | 13.2% | 11.9% | -1.3% |

申购评分在第二轮首次突破 8.0，单看申购维度已达优秀线。

### 10.4 优化路径总结

```
基线 4.96 分
    │
    │ 第一轮：特征工程 + 集成建模 + 早停裁剪
    ↓
第一轮 8.05 分（+3.09）
    │
    │ 第二轮：超参数调优（学习率 / 树数 / 集成权重）
    ↓
第二轮 8.14 分（+0.09）
```

两轮优化的核心收益来自第一轮的特征工程（月初月末连续特征贡献最大），第二轮的超参调优属于精修，收益较小但稳定。若要继续提升至 9 分以上，需要引入 XGBoost 三模型集成、ARIMA 预测外部变量、时间序列交叉验证调参等更复杂手段。

### 10.5 第三轮修复：数据泄露与验证逻辑合规

经审查，前两轮的 8.14 分存在三个严重问题，导致分数虚高、不符合指导书要求。本轮做彻底修复，得到真实可信的验证分数。

**问题一：数据泄露（致命）**

`src/preprocess.py` 中存在 6 个用当天真实值派生的特征：

| 特征 | 问题 |
|---|---|
| `purchase_redeem_ratio` | 当天申购 / 当天赎回 |
| `purchase_diff_lag1` | 当天申购 - 昨天申购 → 已知昨天申购，等价于泄露当天申购 |
| `redeem_diff_lag1` | 当天赎回 - 昨天赎回 → 同上 |
| `purchase_diff_lag7` | 当天申购 - 7天前申购 |
| `redeem_diff_lag7` | 当天赎回 - 7天前赎回 |
| `net_inflow` | 当天申购 - 当天赎回 |

模型预测「当天申购」时，输入特征已包含当天申购的间接信息，相当于把答案放进题目。

**问题二：验证集用真实历史滚动（严重）**

`src/train.py` 验证集预测时直接从 `valid_df` 提取特征，所有 lag/rolling 特征都基于 8 月真实历史计算。但 9 月预测时是真正的滚动预测（预测值回填为下一天 lag），验证集比真实 9 月预测容易得多。

**问题三：评分是自定义模拟公式（中等）**

指导书明确「误差与得分之间的计算公式不公布」，只保证 error=0 得 10 分、error>0.3 得 0 分、单调递减。项目采用线性近似公式 `score = max(0, 10×(1-error/0.3))`，8.14 是该公式下的模拟分数，非官方成绩。

**修复方案**：

1. **修复问题一**：删除 6 个泄露特征，改为用 `shift` 派生的安全版本（如 `purchase_diff_lag1_hist = 今天.shift(1) - 今天.shift(2)`），特征数从 113 降到 107
2. **修复问题二**：`src/train.py` 验证集改为滚动预测逻辑，逐天预测 8 月 1 日→8 月 31 日，预测值回填为下一天 lag 特征，与 9 月预测完全一致
3. **修复问题三**：代码保留线性近似公式（指导书允许，官方公式不公开），但文档明确标注「模拟分数，非官方成绩」

**修复后的真实分数**：

| 指标 | 修复前（虚高） | 修复后（合规） | 变化 |
|---|---|---|---|
| 申购 MAPE | 6.01% | 15.55% | +9.54% |
| 赎回 MAPE | 5.22% | 16.45% | +11.23% |
| 申购评分 | 8.00 | 5.21 | -2.79 |
| 赎回评分 | 8.26 | 5.10 | -3.16 |
| **总分** | **8.14** | **5.15** | **-2.99** |
| 申购零分日（>30%） | 0 天 | 4 天 | +4 |
| 赎回零分日（>30%） | 0 天 | 5 天 | +5 |

**分数下降说明**：修复后分数下降是预期的，因为去掉了数据泄露和验证集易化。5.15 是在合规条件下的真实表现，主要误差来自月初月末效应（8月1日申购误差 51%、8月31日申购误差 37%）和滚动预测的累积误差。

**合规后的进一步调优**：

在合规基础上，通过本地调参实验脚本遍历 11 种参数组合，找到最优配置：

| 参数 | 修复前 | 最优值 |
|---|---|---|
| 集成权重 | LightGBM 0.95 + RF 0.05 | 纯 LightGBM（权重 1.0） |
| 平滑系数 | 0.85 | 0.95 |

调优后分数从 5.07 提升到 5.15（+0.08），收益有限。主要瓶颈是零分日（>30% 误差），需要针对性优化月初月末效应，属于后续工作。

### 10.6 优化路径总结（完整版）

```
基线 4.96 分
    │
    │ 第一轮：特征工程 + 集成建模 + 早停裁剪
    ↓
第一轮 8.05 分（+3.09，含数据泄露）
    │
    │ 第二轮：超参调优（学习率 / 树数 / 集成权重）
    ↓
第二轮 8.14 分（+0.09，含数据泄露）
    │
    │ 第三轮：修复数据泄露 + 验证集改滚动预测 + 文档标注评分近似
    ↓
第三轮 5.15 分（-2.99，合规真实分数）
    │
    │ 第四轮：多种子集成 + 小学习率+多树 + 平滑系数精调
    ↓
第四轮 5.19 分（+0.04，合规调优极限）
```

前两轮的 8.14 分是建立在数据泄露和验证集易化基础上的虚高数字，第三轮修复后得到真实可信的 5.15 分。虽然分数下降，但符合指导书要求，验证集分数真正反映了模型在未来 30 天上的预测能力。第四轮在合规基础上通过多种子集成等手段微调到 5.19，但提升空间有限。

### 10.7 第四轮合规优化尝试与极限分析

第三轮修复后得到合规真实分数 5.15，本轮目标是在**严格保持合规**（无数据泄露、验证集滚动预测）的前提下进一步提升分数。尝试了 7 类策略：

| 策略 | 描述 | 结果 | 是否采用 |
|------|------|------|----------|
| S1 多种子集成 | 3个不同随机种子的 LightGBM 平均 | 5.146→5.150 | 采用 |
| S2 月初月末修正 | 用历史月初1日均值加权修正预测 | 5.15→5.05 | 不采用（降分） |
| S3 同星期几修正 | 用历史同星期几均值加权修正 | 5.15→4.81 | 不采用（降分） |
| S4 平滑系数精调 | 0.85→0.99，减少历史均值拉扯 | 5.15→5.19 | 采用 |
| S5 基线混合 | 模型预测 × 同星期几4周均值 | 5.19→5.16 | 不采用（降分） |
| S6 样本权重 | 时间衰减权重，近期样本权重高 | 5.19→4.71 | 不采用（降分） |
| S7 增强特征 | 月内进度、工资日、8周同星期几等 | 5.19→5.09 | 不采用（降分） |

**S1 多种子集成 + S4 平滑系数精调** 是唯二有效策略，组合后达到 5.19。其他策略均降分。

**极限分析**：

1. **零分日瓶颈**：验证集31天中，申购有4天、赎回有5天误差>30%（零分日）。这些天主要是月初1日（8月1日申购误差 52%）、月末（8月31日）、周日异常低值。常规优化无法解决。

2. **滚动预测累积误差**：第1天预测错→第2天 lag_1 特征错→第2天预测也错。这种级联效应使得后期漂移严重。

3. **8月数据特殊性**：8月有特殊资金波动（可能与暑期、月末效应相关），历史数据难以捕捉。

4. **基线对照**：同星期几4周滚动均值基线 4.76，模型 5.19 仅高 0.43。说明模型确实有效，但提升空间有限。

**8.0 目标的可行性**：

要达到 8.0 分（线性近似公式下），MAPE 需要从 16% 降到 6%，误差减少 60%+。在严格合规条件下（无泄露、滚动预测），这几乎不可能实现。任何接近 8.0 的分数必然依赖：
- 使用当天真实值派生特征（数据泄露，违反指导书）
- 验证集用真实历史而非滚动预测（验证集易化）
- 使用非官方评分公式（虚高分数）

第三轮已彻底放弃这些作弊手段，5.19 是当前技术下的合规极限。

**合规优化极限的应对**：

接受 5.19 作为合规真实分数，文档继续标注「线性近似公式模拟值，非天池官方成绩」。该分数虽然不高，但符合指导书要求，是模型在未来30天上真实预测能力的反映。

---

*如有任何环节不清楚，随时可以问我继续推进。*