# 项目说明书：资金流入流出预测系统

> 本文档为项目全局导览，旨在帮助新读者在 10 分钟内快速掌握项目做什么、怎么跑、核心实现是什么。详细技术细节请参阅 `README.md`，需求背景与设计思路请参阅 `项目二_需求分析与最小方案.md`。

---

## 一、项目是什么

基于天池「资金流入流出预测」赛题，预测余额宝 **2014 年 9 月 1~30 日**每天的**申购总额**和**赎回总额**。

- **输入**：2013-07-01 ~ 2014-08-31 的用户交易数据 + 收益率 + Shibor 利率（共 4 张表）
- **输出**：30 天的 `report_date, purchase, redeem` 提交文件
- **评估**：每日相对误差 MAPE，采用线性近似公式 `score = max(0, 10×(1-error/0.3))`（官方公式不公开），总分 = 申购得分×45% + 赎回得分×55%
- **当前验证集效果**：总分 **5.43 / 10**（申购 MAPE 13.55%，赎回 MAPE 15.93%，合规滚动预测）

---

## 二、目录结构速览

```
Purchase-Redemption/
├── Purchase Redemption Data/      # 原始赛题数据（不提交 Git，需自行解压）
├── src/                            # 核心源码
│   ├── config.py                   # 路径配置
│   ├── data_loader.py              # 数据读取
│   ├── preprocess.py               # 预处理 + 特征工程（113个特征）
│   ├── train.py                    # LightGBM 训练（滚动预测评估）
│   ├── predict.py                  # 9月滚动预测
│   └── evaluate.py                 # 评估函数
├── output/                         # 运行结果（运行后生成）
│   ├── daily_features.csv          # 预处理后的日级宽表
│   ├── validation_prediction.csv    # 8月验证集预测
│   ├── tc_comp_predict_table.csv   # 9月提交文件（无表头）
│   └── tc_comp_predict_table_with_header.csv
├── models/                         # 模型文件（运行后生成）
│   ├── model_purchase.pkl          # 申购模型
│   ├── model_redeem.pkl            # 赎回模型
│   └── feature_columns.json        # 特征列名契约
├── app.py                          # Streamlit 可视化入口
├── run_all.py                      # 一键运行完整流程
├── requirements.txt                # Python 依赖
├── README.md                       # GitHub 项目说明
├── 项目二_需求分析与最小方案.md       # 需求分析与设计文档
└── LICENSE
```

---

## 三、运行流程

整个项目从原始数据到最终预测的链路如下：

```
原始数据 (Purchase Redemption Data/)
    ↓
[src/preprocess.py]  聚合每日总量 + 构造113个特征
    ↓
output/daily_features.csv
    ↓
[src/train.py]  训练多种子 LightGBM 集成模型（8月滚动预测评估）
    ↓
models/model_purchase.pkl + model_redeem.pkl + feature_columns.json
    ↓
output/validation_prediction.csv（8月验证集评估）
    ↓
[src/predict.py]  9月30天滚动预测（预测值回填为下一天lag特征）
    ↓
output/tc_comp_predict_table.csv（最终提交文件）
    ↓
[app.py]  Streamlit 可视化展示
```

**三个阶段一句话概括**：

1. **预处理**：把 280 万条用户交易聚合成 427 天的日级数据，再构造 113 个特征
2. **训练**：用 365 天训练，3个不同随机种子的 LightGBM 集成，预测时取平均。验证集采用滚动预测（与9月一致），避免使用8月真实历史导致分数虚高
3. **预测**：加载模型，逐天滚动预测 9 月 30 天，输出赛题格式文件

---

## 四、快速开始

### 4.1 环境准备

- Python 3.8+
- 把天池数据解压到 `Purchase Redemption Data/` 目录（必须有 `user_balance_table.csv`）

```bash
pip install -r requirements.txt
```

### 4.2 一键运行

```bash
python run_all.py
```

运行完成后，9 月预测结果在 `output/tc_comp_predict_table.csv`。

### 4.3 可视化查看

```bash
streamlit run app.py
```

浏览器打开后可看到 5 个页面：数据探索、模型评估、预测结果、误差分析、课程要求对照。

---

## 五、核心实现

### 5.1 特征工程（113 个特征）

| 特征类别 | 作用 |
|---|---|
| 周期傅里叶编码 | dayofweek/dayofmonth/month 的 sin/cos，让模型感知周期连续性 |
| 月初月末连续特征 | 解决月初、月末误差爆炸问题（权重衰减式特征） |
| 节假日特征 | 2014 年节假日前后资金异动捕捉 |
| 历史同期特征 | 同月同日、同周几、近 4 周同周几均值 |
| 比率差分特征 | 申购赎回比率、一阶差分、净流入 |
| 滚动统计增强 | lag + rolling mean/std/min/max/median |
| 外部变量 | 收益率、Shibor 利率 |

### 5.2 模型与预测

- **多种子 LightGBM**：树模型，擅长表格数据，支持早停。第四轮调优发现纯 LightGBM + 3种子集成最优，RandomForest 保留为降级接口但最终未启用
- **目标变换**：训练时 `log1p`，预测时 `expm1`，处理上亿分的大数值
- **早停**：100 轮验证集不提升则停止，自动找最优迭代轮数
- **预测裁剪**：裁剪到训练集 P1~P99 分位数区间，避免极端值

### 5.3 滚动预测

9 月真实值未知，只能逐天预测：

- 第 1 天：用 8 月最后一天的特征预测
- 第 2 天：把第 1 天的预测值回填为 lag 特征，再预测
- 依此类推到第 30 天
- 9 月的外部变量（收益率、Shibor）用 8 月均值填充
- 平滑修正：`预测值 × 0.99 + 近7日均值 × 0.01`，缓解滚动漂移
- 月末3天上调：申购×1.2、赎回×1.3，缓解月末系统性低估

---

## 六、验证集效果

用 2014 年 8 月做验证集，模拟 30 天预测：

| 指标 | 数值 |
|---|---|
| 申购 MAPE | 13.55% |
| 赎回 MAPE | 15.93% |
| 申购评分 | 5.76 |
| 赎回评分 | 5.16 |
| **总分** | **5.43 / 10** |
| 申购零分日（误差>30%） | 2 天 |
| 赎回零分日（误差>30%） | 4 天 |

**关于分数**：验证集采用与 9 月一致的滚动预测，无数据泄露。线性近似公式下的模拟分数为 5.43，官方公式不公开，该分数仅仅为内部模拟值，采用10 × (1-max（相对误差，0.3） ÷ 0.3)。

优化路径：

- **基线 → 8.14**：特征工程扩展 + 集成建模 + 超参调优（早期版本，存在数据泄露和验证集易化问题）
- **8.14 → 5.15**：第三轮合规修复，删除泄露特征、验证集改为滚动预测，得到真实可信分数
- **5.15 → 5.19**：第四轮调优，多种子集成 + 小学习率+多树 + 平滑系数精调
- **5.19 → 5.43**：第五轮优化，月末3天上调（申购×1.2、赎回×1.3）救回3个零分日

### 6.1 验证集异常点分析（剩余6个零分日）

应用月末3天上调后，验证集仍有 6 个零分日（误差>30%）。这些点是 8 月特有的资金异动，基于历史数据无法精准预测，分三类：

**类型A：月初1日异常高（申购）**

| 日期 | 真实值 | 模型预测 | 误差 | 偏离方向 |
|------|--------|----------|------|----------|
| 8月1日 | 3.749亿 | 1.767亿 | 52.9% | 低估 |

历史所有月初1日申购均值仅2.13亿、中位数1.83亿，8月1日3.749亿是历史均值的1.76倍。月初1日有资金集中流入效应（工资发放、月初理财申购），但模型按近7日均值平滑后系统性低估。曾尝试月初1日单独上调×1.3~2.0，虽可救回8月1日，但对历史月初1日（从0.19亿到4.53亿波动14倍）误差极大，属于针对验证集单一数据点调参，判定为过拟合未采用。

**类型B：月末异常高（赎回）**

| 日期 | 真实值 | 模型预测 | 误差 | 偏离方向 |
|------|--------|----------|------|----------|
| 8月27日 | 4.682亿 | 2.760亿 | 41.0% | 低估 |

历史月末3天赎回均值2.10亿，8月27日4.68亿是历史月末均值的2.2倍。月末资金集中流出（月末理财赎回、账单结算），模型未充分捕捉月末效应强度。月末3天上调×1.3已救回8月29日、8月31日等多数月末日，但8月27日偏离过远无法救回。

**类型C：周日波动大（赎回与申购方向不一致）**

| 日期 | 类型 | 真实值 | 模型预测 | 误差 | 偏离方向 |
|------|------|--------|----------|------|----------|
| 8月3日 | 赎回 | 1.271亿 | 1.977亿 | 55.6% | **高估** |
| 8月10日 | 申购 | 2.595亿 | 1.750亿 | 32.6% | 低估 |
| 8月17日 | 赎回 | 1.396亿 | 1.842亿 | 32.0% | **高估** |

8月5个周日赎回从1.27亿到2.93亿波动剧烈，且方向不一致：3日和17日异常低（模型高估），31日异常高（模型低估已救回）。10日申购异常高但赎回正常。周日资金行为不规则，无法用统一系数修正。

**剩余1个孤立零分日**

| 日期 | 类型 | 真实值 | 模型预测 | 误差 | 偏离方向 |
|------|------|--------|----------|------|----------|
| 8月8日 | 赎回 | 3.116亿 | 2.021亿 | 35.2% | 低估 |

8月8日（周五）赎回3.116亿，接近7月周四均值3.08亿，是孤立高值，无周期规律可循。

**异常点共性问题**

1. **偏离历史均值70%以上**：8月1日申购（+76%）、8月27日赎回（+123%）、8月3日赎回（-31%）等都远超历史波动范围
2. **方向不一致**：同为周日，3日和17日低估自身（高估误差），31日高估自身（低估误差），无法统一修正
3. **孤立性**：8月8日等孤立高值无周期规律
4. **年度增长**：2013年8月周日赎回才1500万级别，2014年8月周日赎回到1.4亿级别，增长10倍，历史数据难以捕捉快速增长

这些异常点是 8 月特有的资金异动，基于历史数据的任何固定系数修正都难让误差降到 30% 以下。要进一步提升需引入外部信息（如收益率曲线、资金政策变化等），已超出当前项目范围。

---

## 七、课程要求对照

| 指导书要求 | 当前实现 | 完成情况 |
|---|---|---|
| 整合四组数据表 | 读取 `user_balance_table`、`user_profile_table`、`mfd_day_share_interest`、`mfd_bank_shibor` | 已完成 |
| 数据清洗、特征工程、数据集成 | 日期解析、日级聚合、收益率/Shibor合并、113个历史特征 | 已完成 |
| 余额一致性关注 | 文档记录 `tBalance = yBalance + total_purchase_amt - total_redeem_amt` 业务约束 | 已说明 |
| 预测2014年9月30天申购/赎回 | `src/predict.py` 逐天滚动预测并输出赛题格式CSV | 已完成 |
| 重点优化赎回预测（55%权重） | 赎回独立建模，针对赎回零分日分析并采用月末赎回×1.3后处理 | 已完成 |
| 数据探索可视化 | 展示历史申购/赎回趋势 | 已完成 |
| 模型训练与评估展示 | 展示验证集指标、优化路径、模型配置 | 已完成 |
| 预测结果展示 | 展示9月预测表格与折线图，并提供无表头提交文件下载 | 已完成 |
| 误差分析模块 | 展示每日相对误差、高估/低估方向、零分日与异常原因 | 已完成 |

**用户画像表使用说明**：项目读取了 `user_profile_table`，但最终任务是日级总量预测。用户性别、城市、星座等静态画像难以直接映射到未来每天的申购/赎回总量；若强行聚合，容易引入噪声。因此当前主模型以交易历史、时间周期、收益率和 Shibor 为主特征，用户画像作为数据资产保留并在文档中说明其边界。

---

## 八、技术栈

| 环节 | 工具 |
|---|---|
| 数据处理 | Python + Pandas + NumPy |
| 建模 | LightGBM |
| 可视化 | Streamlit + Plotly |
| 模型持久化 | joblib |

---

## 九、进一步阅读

| 想了解 | 阅读 |
|---|---|
| 项目背景、数据表结构、赛题评估规则 | `项目二_需求分析与最小方案.md`（第一至九章） |
| 实际实现细节、多轮优化过程 | `项目二_需求分析与最小方案.md`（第十章） |
| 目录结构、安装运行、代码流程 | `README.md` |
| 本文档（快速上手导览） | `项目说明书.md` |

---

*读完本文档约需 10 分钟，足以掌握项目全貌并跑通流程。如需深入某个模块，按上表查阅对应文档。*
