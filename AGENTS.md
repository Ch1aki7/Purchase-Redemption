# Purchase-Redemption 项目协作约束

本文件适用于整个仓库。实现路线与进度以 `Documents/结构模型与日历建模迭代工作文档.md` 为准。

## 不可破坏的边界

1. 任何预测日当天才能观测到的金额、人数、用户分群或派生值，不得作为该日模型输入。
2. 用户分类只能使用预测截点之前的行为；滚动回测的每一折必须独立遵守该截点。
3. 参数搜索使用开发月份，2014-08 作为独立确认月；最终确认后才可用截至 2014-08-31 的全量数据重训。
4. 不得用项目自定义模拟分冒充天池官方成绩。选型必须同时报告 MAPE、P90 相对误差和严重错误日。
5. 新方案未经统一滚动回测与独立确认，不得覆盖当前正式 XGBoost 输出，只能生成候选文件。
6. 固定月末 `×1.2/×1.3` 只能作为显式实验，不能成为默认策略。

## 模型与数据要求

- 申购、消费、转出应分别建模，赎回通过 `consume + transfer` 恢复并校验。
- 大额用户与普通用户的拆分应可追溯，并保留未拆分总额作为基线。
- 日历变量只能来自预测时已知信息；节假日效应必须由历史折估计，禁止手写目标月倍率。
- STL/ARIMA、相似日期、机器学习模型的预测都必须保存逐日 OOF 结果，融合权重只能从 OOF 学习。
- 预处理后继续验证余额、用户分群和资金流组成的恒等关系。

## 前端要求

Streamlit 页面必须保持以下四个一级板块：

1. 数据探索
2. 模型训练与评估
3. 预测结果展示
4. 误差分析

页面应优先展示与预测有关的时间序列、资金流组成、用户分群、日历效应和误差诊断。静态画像或弱相关图表只能放入折叠区或删除。会话内调参不得覆盖正式模型和正式提交文件。

## 每次迭代完成前

- 更新工作文档中的任务状态、实验表和变更日志。
- 执行语法检查、无泄漏断言、滚动回测和前端无端口加载测试。
- 明确说明哪些结果是正式基线、候选方案和未完成实验。
