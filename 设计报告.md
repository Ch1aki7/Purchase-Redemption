# 资金流入流出预测系统设计报告

## 一、项目概述

本项目对应《大数据技术项目实训》课程指导书中的“项目二：资金流入流出预测系统”。项目基于天池“资金流入流出预测”赛题，使用余额宝历史交易数据、用户信息、收益率和银行间拆借利率等数据，预测 2014 年 9 月 1 日至 9 月 30 日每天的资金申购总额和赎回总额。

项目最终输出一个符合赛题格式的预测文件：

```text
report_date,purchase,redeem
20140901,预测申购金额,预测赎回金额
...
20140930,预测申购金额,预测赎回金额
```

其中：

- `purchase` 表示当天总申购金额；
- `redeem` 表示当天总赎回金额；
- 金额单位为“分”；
- 最终需要预测 2014 年 9 月共 30 天的数据。

本项目不仅完成了预测模型，还实现了一个基于 Streamlit 的可交互可视化系统，用于展示数据规律、训练模型、查看预测结果和分析误差。

---

## 二、指导书要求与项目实现对照

课程指导书对本项目提出了四类核心要求：数据预处理、预测模型构建、可交互可视化系统、误差评价与分析。下表展示项目实现与课程要求的对应关系。

| 指导书要求 | 项目实现 |
|---|---|
| 整合四组数据表 | 读取用户信息表、用户申购赎回表、收益率表和 Shibor 利率表 |
| 数据清洗与特征工程 | 完成日期解析、日级聚合、缺失值处理、时间特征、滞后特征、滚动特征、节假日特征、收益率与利率特征构造 |
| 预测未来 30 天申购和赎回 | 使用历史数据训练模型，滚动预测 2014 年 9 月 1 日至 9 月 30 日每日 `purchase` 和 `redeem` |
| 重点优化赎回预测 | 赎回单独建模，评分中赎回权重为 55%，并对赎回零分日进行了专项分析 |
| 数据探索可视化 | 展示历史申购/赎回趋势、用户画像分布、收益率变化、Shibor 利率变化 |
| 模型训练与评估可视化 | 页面中可选择 RandomForest、GradientBoosting、LightGBM 等模型和参数，并真实训练模型 |
| 预测结果展示 | 展示 9 月 30 天预测表格与折线图，并提供提交文件下载 |
| 误差分析 | 展示每日绝对误差、相对误差、当日得分、零分日高亮和改进方向 |

项目主流程采用多种子 LightGBM 集成进行正式预测；可视化系统中额外提供交互式模型训练功能，方便用户在网页中选择模型和参数进行对比实验。

---

## 三、数据来源与数据表说明

项目使用天池“资金流入流出预测”赛题提供的四张数据表。

### 3.1 用户信息表 `user_profile_table`

该表记录用户的基础画像信息。

| 字段 | 含义 |
|---|---|
| `user_id` | 用户编号 |
| `sex` | 性别 |
| `city` | 城市编码 |
| `constellation` | 星座 |

在本项目中，该表主要用于可视化系统中的用户分布展示，例如性别分布、城市 Top 分布和星座分布。由于最终任务是预测每日全平台总申购额和总赎回额，用户静态画像难以直接稳定映射到未来每日资金总量，因此主模型没有直接使用用户画像作为核心预测特征。

### 3.2 用户申购赎回表 `user_balance_table`

这是项目中最核心的数据表，记录用户每天的余额、申购和赎回行为。

| 字段 | 含义 |
|---|---|
| `user_id` | 用户编号 |
| `report_date` | 日期 |
| `tBalance` | 今日余额 |
| `yBalance` | 昨日余额 |
| `total_purchase_amt` | 今日总申购金额 |
| `total_redeem_amt` | 今日总赎回金额 |
| `direct_purchase_amt` | 直接申购金额 |
| `purchase_bal_amt` | 支付宝余额申购金额 |
| `purchase_bank_amt` | 银行卡申购金额 |
| `consume_amt` | 消费金额 |
| `transfer_amt` | 转账金额 |
| `share_amt` | 当日收益 |

指导书中特别强调需要关注余额一致性：

```text
tBalance = yBalance + total_purchase_amt - total_redeem_amt
```

该关系反映了资金流入、流出与余额变化之间的业务逻辑。项目在设计文档中保留了该约束说明，并在数据处理阶段以 `user_balance_table` 为核心数据源，按日期聚合生成每日总申购和总赎回序列。

### 3.3 收益率表 `mfd_day_share_interest`

该表记录余额宝每日收益率信息。

| 字段 | 含义 |
|---|---|
| `mfd_date` | 日期 |
| `mfd_daily_yield` | 万份收益 |
| `mfd_7daily_yield` | 7 日年化收益率 |

收益率会影响用户申购和赎回行为，因此项目将其合并到日级特征表中，并在可视化系统中展示收益率变化曲线。

### 3.4 银行间拆借利率表 `mfd_bank_shibor`

该表记录不同期限的 Shibor 利率。

| 字段 | 含义 |
|---|---|
| `Interest_O_N` | 隔夜利率 |
| `Interest_1_W` | 1 周利率 |
| `Interest_2_W` | 2 周利率 |
| `Interest_1_M` | 1 月利率 |
| `Interest_3_M` | 3 月利率 |
| `Interest_6_M` | 6 月利率 |
| `Interest_9_M` | 9 月利率 |
| `Interest_1_Y` | 1 年利率 |

Shibor 反映市场资金价格变化，可能影响用户资金流动。项目将 Shibor 数据作为外部变量合并进模型特征，并在数据探索页面中提供利率变化图。

---

## 四、数据预处理与特征工程设计

### 4.1 日级聚合

原始 `user_balance_table` 是用户级交易记录，一天内会有多名用户的多条记录。预测目标是每天全平台申购和赎回总额，因此首先按日期聚合：

```text
purchase = sum(total_purchase_amt)
redeem   = sum(total_redeem_amt)
```

同时保留每日用户数和记录数，作为辅助统计信息。

### 4.2 日期与时间特征

资金流动具有明显的时间周期性，例如周末、月初、月末、节假日前后都会影响用户行为。项目构造了以下时间特征：

| 特征类型 | 作用 |
|---|---|
| `dayofweek` | 表示星期几，捕捉周周期 |
| `dayofmonth` | 表示月内第几天，捕捉月初/月末效应 |
| `month` | 表示月份，捕捉月度季节性 |
| `is_weekend` | 标记周末 |
| `is_month_start` / `is_month_end` | 标记月初/月末 |
| 傅里叶编码 | 用 `sin/cos` 表示周期，使周期变化更平滑 |

傅里叶编码的作用是避免简单整数编码造成“1 日”和“31 日”在数值上相距很远的问题，使模型更容易学习周期规律。

### 4.3 月初月末特征

资金流动在月初和月末更容易出现异常。例如月初可能有工资到账后申购，月末可能有账单结算或理财赎回。因此项目增加：

- 距月初天数；
- 距月末天数；
- 是否月初 3 天；
- 是否月末 3 天；
- 是否月初 7 天；
- 是否月末 7 天；
- 月初/月末指数衰减权重。

这些特征帮助模型识别月内位置对申购和赎回的影响。

### 4.4 滞后特征与滚动统计特征

时间序列预测不能使用未来信息，只能使用过去已经发生的数据。因此项目使用历史值构造滞后特征：

| 特征 | 含义 |
|---|---|
| `purchase_lag_1` | 前 1 天申购 |
| `purchase_lag_7` | 前 7 天申购 |
| `redeem_lag_1` | 前 1 天赎回 |
| `redeem_lag_7` | 前 7 天赎回 |

同时构造滚动窗口统计：

- 近 3 天均值；
- 近 7 天均值；
- 近 14 天均值；
- 近 30 天均值；
- 滚动标准差、最小值、最大值和中位数。

这些特征用于描述近期资金流趋势和波动程度。

### 4.5 历史同期特征

为了捕捉“同星期几”和“上月同日”的规律，项目构造：

- 上月同日申购/赎回；
- 过去 1 至 4 周同星期几的申购/赎回；
- 过去 4 周同星期几均值。

这类特征对周周期和月周期明显的时间序列较为有效。

### 4.6 防止数据泄露

在时间序列预测中，最重要的原则是不能使用预测当天的真实值构造特征。项目早期曾存在部分使用当天申购/赎回派生的特征，后来已经删除并替换为基于历史 `shift` 的安全特征。

例如：

```text
错误做法：当天申购 - 昨天申购
正确做法：昨天申购 - 前天申购
```

最终特征均基于历史数据或外部变量，避免把预测答案提前泄露给模型。

---

## 五、预测模型设计

### 5.1 任务拆分

项目需要同时预测两个目标：

- 每日申购总额 `purchase`；
- 每日赎回总额 `redeem`。

由于申购和赎回虽然相关，但变化规律不同，因此项目采用两个独立回归模型分别预测。

### 5.2 主流程模型：多种子 LightGBM 集成

正式预测主流程使用多种子 LightGBM 集成。具体做法是：

1. 使用多个不同随机种子训练 LightGBM 模型；
2. 每个模型都预测同一天的申购或赎回；
3. 对多个模型的预测结果取平均。

这样可以降低单一模型随机波动，提高预测稳定性。

### 5.3 目标值变换

每日申购和赎回金额通常是上亿级别，数值较大。为了降低方差、稳定模型训练，项目对目标值做 `log1p` 变换：

```text
训练目标 = log(真实值 + 1)
```

预测完成后再使用 `expm1` 还原：

```text
预测值 = exp(模型输出) - 1
```

### 5.4 滚动预测

9 月真实值未知，因此不能直接使用 9 月真实历史构造滞后特征。项目采用滚动预测：

```text
预测 9 月 1 日
→ 将 9 月 1 日预测值加入历史序列
→ 用更新后的历史序列预测 9 月 2 日
→ 依次预测到 9 月 30 日
```

8 月验证集也采用同样逻辑，这样验证结果更接近真实预测场景。

### 5.5 月末后处理

实验发现模型对 8 月月末 3 天存在系统性低估，因此最终加入温和的月末上调策略：

```text
月末 3 天：申购 × 1.2，赎回 × 1.3
```

该策略使验证集模拟总分从 5.19 提升到 5.43，零分日从 9 个减少到 6 个。

需要说明的是，后续滚动回测显示该规则在其他月份不一定稳定，因此报告中将其作为当前 8 月验证集上的有效后处理，同时明确存在一定过拟合风险。

---

## 六、验证策略与评分方法

### 6.1 数据划分

项目使用 2014 年 8 月作为本地验证集：

| 数据范围 | 用途 |
|---|---|
| 2013-08-01 至 2014-07-31 | 模型训练 |
| 2014-08-01 至 2014-08-31 | 本地验证 |
| 2014-09-01 至 2014-09-30 | 最终预测 |

8 月验证集采用与 9 月一致的滚动预测逻辑，避免使用 8 月真实历史造成验证分数虚高。

### 6.2 相对误差

指导书要求使用每日相对误差衡量预测准确度：

```text
申购误差 = |预测申购 - 真实申购| / 真实申购
赎回误差 = |预测赎回 - 真实赎回| / 真实赎回
```

### 6.3 模拟评分函数

指导书说明：

- 误差为 0 时，当日得分为 10；
- 误差大于 0.3 时，当日得分为 0；
- 得分函数单调递减；
- 具体公式不公开。

因此项目使用线性近似函数作为本地模拟评分：

```text
score = max(0, 10 × (1 - error / 0.3))
```

最终总分按照指导书权重计算：

```text
总分 = 申购得分 × 45% + 赎回得分 × 55%
```

其中赎回权重更高，因此项目在误差分析中重点关注赎回预测。

---

## 七、可交互可视化系统设计

项目使用 Streamlit 实现 Web 可交互可视化系统。系统包含四个主要模块，分别对应指导书要求。

### 7.1 数据探索模块

该模块用于帮助用户理解原始数据规律，包含：

1. 历史申购/赎回趋势图；
2. 收益率变化图；
3. Shibor 利率变化图；
4. 用户画像分布图，包括性别、城市 Top 和星座分布。

通过该模块，用户可以观察资金流动是否存在周期性、趋势性和异常波动。

### 7.2 模型训练与评估模块

该模块允许用户直接在网页中选择模型和参数并真实训练。支持的模型包括：

- RandomForestRegressor；
- GradientBoostingRegressor；
- LightGBM（环境中安装 LightGBM 时可用）。

用户可以调整：

- 树数量；
- 最大深度；
- 学习率；
- 随机种子。

点击“训练并更新展示结果”后，系统会在当前 Streamlit 会话中训练申购模型和赎回模型，并展示：

1. 申购 MAPE；
2. 赎回 MAPE；
3. 模拟总分；
4. 零分日数量；
5. 训练过程损失曲线；
6. 时间序列交叉验证结果。

训练结果会保存到 `st.session_state` 中，用户可以在侧边栏选择查看“主流程滚动预测结果”或“本次交互训练结果”。

### 7.3 预测结果展示模块

该模块展示两类结果：

1. 2014 年 9 月未来 30 天预测结果；
2. 2014 年 8 月验证集真实值与预测值对比。

展示方式包括：

- 表格；
- 折线图；
- 无表头提交文件下载；
- 每日误差和每日得分。

这样既能查看最终提交结果，也能通过验证集判断预测是否可靠。

### 7.4 误差分析模块

该模块用于分析模型预测中误差较大的日期。展示内容包括：

- 每日申购绝对误差；
- 每日赎回绝对误差；
- 每日申购相对误差；
- 每日赎回相对误差；
- 每日申购得分；
- 每日赎回得分；
- 当日加权得分；
- 高估/低估方向；
- 零分日高亮；
- 异常点类型；
- 改进方向提示。

当某天相对误差超过 30% 时，该天得分为 0，系统会将其列为零分日，方便用户快速定位问题。

---

## 八、实验结果与模型效果

### 8.1 当前主流程验证结果

在 2014 年 8 月验证集上，当前主流程结果如下：

| 指标 | 数值 |
|---|---:|
| 申购 MAPE | 13.55% |
| 赎回 MAPE | 15.93% |
| 申购评分 | 5.76 |
| 赎回评分 | 5.16 |
| 模拟总分 | 5.43 |
| 申购零分日 | 2 天 |
| 赎回零分日 | 4 天 |

该结果是在无数据泄露、验证集滚动预测条件下得到的模拟分数。

### 8.2 优化过程说明

项目早期通过扩展特征和模型调参曾得到 8.14 的验证分数，但后来发现该结果存在两个问题：

1. 部分特征使用了当天真实申购/赎回派生信息，存在数据泄露；
2. 验证集使用了 8 月真实历史构造滞后特征，比真实 9 月预测更容易。

修复后，验证集分数下降到 5.15，但这才是更接近真实预测场景的结果。随后通过多种子 LightGBM、平滑系数调整和月末后处理，将分数提升到 5.43。

### 8.3 滚动回测补充

为了检验月末上调策略是否只对 8 月有效，项目增加了滚动回测。快速单种子回测结果显示，月末上调策略虽然改善了 8 月表现，但在部分月份会下降，说明该策略存在一定验证月过拟合风险。因此项目在报告中如实说明：5.43 是 8 月验证集上的当前主流程模拟结果，不应简单等同于真实官方成绩。

---

## 九、异常点与误差分析

当前主流程在 8 月验证集上仍有 6 个零分日。这些点大多属于历史数据难以准确预测的异常波动。

### 9.1 月初异常

8 月 1 日申购真实值为 3.749 亿，而模型预测约 1.767 亿，误差超过 50%。历史月初 1 日申购均值约 2.13 亿，8 月 1 日明显偏高。虽然可以通过提高月初系数救回该点，但这种做法容易针对单一验证日过拟合，因此最终未采用。

### 9.2 月末异常

8 月 27 日赎回真实值为 4.682 亿，而模型预测约 2.760 亿，误差超过 40%。该日属于月末附近异常赎回高值。月末 3 天后处理已经救回部分月末零分日，但无法覆盖所有月末附近异常。

### 9.3 周日波动

8 月 3 日和 8 月 17 日赎回偏低，模型高估；而 8 月 31 日赎回偏高，模型低估。说明周日资金流动方向并不一致，难以使用统一系数修正。

### 9.4 孤立高值

8 月 8 日赎回异常偏高，但没有明显月初、月末或周末规律，属于孤立高值。此类异常需要外部业务信息辅助判断，仅依靠历史序列难以准确预测。

---

## 十、项目运行方式

### 10.1 安装依赖

```bash
pip install -r requirements.txt
```

### 10.2 运行完整预测流程

```bash
python run_all.py
```

该命令会依次执行：

1. 数据预处理与特征工程；
2. 模型训练与 8 月验证；
3. 9 月 30 天滚动预测。

### 10.3 启动可视化系统

```bash
streamlit run app.py
```

启动后浏览器中可以看到：

1. 数据探索；
2. 模型训练与评估；
3. 预测结果展示；
4. 误差分析。

### 10.4 输出文件

| 文件 | 说明 |
|---|---|
| `output/daily_features.csv` | 日级特征表 |
| `output/validation_prediction.csv` | 8 月验证集预测结果 |
| `output/tc_comp_predict_table.csv` | 9 月最终提交文件，无表头 |
| `output/tc_comp_predict_table_with_header.csv` | 9 月预测结果，带表头 |
| `models/model_purchase.pkl` | 申购模型 |
| `models/model_redeem.pkl` | 赎回模型 |
| `models/feature_columns.json` | 特征列顺序 |

---

## 十一、总结与不足

本项目完成了课程指导书要求的资金流入流出预测系统，主要成果包括：

1. 完成四张数据表的读取、清洗、集成与特征工程；
2. 构建申购和赎回两个独立预测模型；
3. 使用滚动预测方式模拟真实未来预测场景；
4. 实现符合赛题格式的 9 月 30 天预测文件；
5. 实现可交互可视化系统，支持数据探索、模型选择训练、预测展示和误差分析；
6. 对异常点和零分日进行了原因分析。

项目也存在一些不足：

1. 官方评分函数不公开，本项目只能使用线性近似函数进行本地模拟；
2. 用户画像对日级总量预测帮助有限，主模型没有直接使用用户画像；
3. 月末后处理对 8 月有效，但滚动回测显示其在其他月份未必稳定；
4. 对孤立异常值的预测能力有限，若要进一步提升，需要引入更多外部业务信息。

总体而言，本项目在保证无数据泄露和真实滚动预测的前提下，完整实现了资金流入流出预测任务，并通过可视化系统将数据规律、模型训练、预测结果和误差分析清晰展示出来，满足课程项目的核心要求。
